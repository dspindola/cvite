# cvite

To install dependencies:

```bash
bun install
```

To run:

```bash
bun codepark
```

or

Compile the source

```bash
bun codepark:compile
```

and run the compiled binary

```bash
codepark
```
